# RISKS — ทะเบียนความเสี่ยง

> รูปแบบ: ความเสี่ยง / โอกาส×ผลกระทบ / วิธีกัน / สัญญาณเตือน
> เรียงตามความร้ายแรงจริง ณ สถานะโค้ดปัจจุบัน

---

## 🔴 R1 — ข้อมูลรั่วข้ามแบรนด์ (tenant leak) — "จบเกม" · **เสี่ยงจริงตอนนี้**
- **โอกาส×ผลกระทบ:** กลาง×สูงสุด — isolation ทำงานจริง (ทุก query ใต้ `tdb()`) แต่ (ก) **ไม่มี rules-test เลย** (ข) **legacy rules ระดับ root ยังไม่ลบ** (`firestore.rules:102-150` เช่น `settings/{doc}` root `read:true`, `/products` root) = ช่อง bypass tenant scope ถ้ามีโค้ด/ข้อมูลใช้ path ระดับ root · แบรนด์ A เห็น/ทับข้อมูล B แม้ครั้งเดียว = trust ตายถาวร
- **วิธีกัน:** Phase 0 — เขียน rules-test รันใน CI ทุก commit + ยืนยันไม่มีใครอ่าน/เขียน root path แล้วลบ legacy block · ห้าม query นอก `tdb()`
- **สัญญาณ:** rules-test แดง · พบ read/write path ที่ไม่ผ่าน `tdb()`/tenant scope
- **หมายเหตุ:** บั๊ก "2 คลัง settings แข่งกัน" (STATUS #2) เป็นคนละเรื่อง — เป็น fail-silent consistency (ทุกอย่างยังอยู่ใต้ `tdb()`) **ไม่ใช่** leak · แก้ใน Phase 0 เช่นกันแต่ไม่ใช่ตัว R1

## 🔴 R2 — สร้างแล้วไม่มีคนใช้ (feature ตายเงียบ) — **เสี่ยงอันดับต้นสำหรับโซโล่**
- **โอกาส×ผลกระทบ:** สูง×สูง — เครื่องมือให้ฟรี + "ลูกค้าจะใช้หรือไม่ก็ได้" + ทีมเล็ก → เสี่ยงลงแรงกับของที่ไม่มีคนแตะ
- **วิธีกัน:** dogfood DemeterRich → ลูกค้านำร่อง 1 ราย ก่อนปล่อยทั้งหมด · เกณฑ์ "เร็ว/คุ้มกว่าทำมือ ณ จังหวะจริงไหม" · feature flag ต่อแบรนด์ ปิดได้ทันที · วัดที่ *ใช้จริง + สั่งผลิตเพิ่ม*
- **สัญญาณ:** สร้างเสร็จแต่ไม่มี log การใช้ · ลูกค้าลองแล้วทิ้ง

## 🟡 R3 — เงิน/QR ผิดพลาด
- **โอกาส×ผลกระทบ:** กลาง×สูง — แม้ v1 เป็น PromptPay QR + อัปสลิปมือ ก็ยังผิดยอด/ยืนยันผิดได้
- **วิธีกัน:** สถานะ "จ่ายแล้ว" ต้องมาจากการยืนยันที่ตรวจสอบได้ (แอดมินดูสลิป) · ปฏิบัติกับ order/สลิปแบบ append-only · ถ้าขยับไป gateway → idempotency key + verify webhook + reconcile รายวัน
- **สัญญาณ:** ยอดออเดอร์ ≠ สลิปจริง · ออเดอร์ค้างสถานะ

## 🟡 R4 — Git/deploy divergence (เจอประจำ)
- **วิธีกัน:** เช็ก deployed == repo ก่อน deploy · snapshot baseline ก่อนต่อยอด · **เลิก patch-pile** (`apply_*.js`) แก้ไฟล์ตรง
- **สัญญาณ:** deployed ≠ repo · patch หาย

## 🟡 R5 — Parser พังจาก format เปลี่ยน (CSV import สินค้า)
- **วิธีกัน:** import แบบลูกค้าอัปโหลดเอง (ไม่ live-sync) · validate ตอน import · **alert เมื่อ format ไม่ตรง แทนกลืนเงียบ** (fail-loud)
- **สัญญาณ:** import "สำเร็จ" แต่ข้อมูลเพี้ยน/หาย

---

## ความเสี่ยงเชิงธุรกิจ

## 🟠 R6 — ความขัดแย้ง แบรนด์โรงงานเอง vs ลูกค้า OEM
- **โอกาส×ผลกระทบ:** กลาง×สูง — โรงงานมีแบรนด์เอง (DemeterRich) อยู่บนแพลตฟอร์มเดียวกับลูกค้า → ลูกค้าระแวง "สูตร/ยอดฉันถูกเอาไปใช้ไหม"
- **วิธีกัน:** กำแพงข้อมูล (R1) ที่เห็นได้ชัด + ทำความโปร่งใสเป็นจุดขาย · แยกเลนแบรนด์เองออกจากข้อมูลลูกค้า
- **สัญญาณ:** ลูกค้าถามเรื่องความลับข้อมูล · churn หลังรู้ว่าโรงงานมีแบรนด์เอง

## 🟠 R7 — เครื่องคำนวณ/คำแนะนำผิด (เมื่อทำ Phase 2)
- **โอกาส×ผลกระทบ:** กลาง×สูง — ถ้าเครื่องมือแนะราคาผิด หรือ (ในอนาคต) แนะอัตราผสมผิด → ความรับผิดย้อนหาโรงงาน
- **วิธีกัน:** เครื่องมือราคาแสดง *สมมติฐาน* ให้เห็น (ราคาโรงงาน+ต้นทุนที่กรอก) · ถ้ามีคำแนะนำเชิงเกษตร ต้องมี disclaimer + ช่วงค่าปลอดภัย · เอกสารมีผลภาษี → ปล่อยให้ระบบบัญชีภายนอก
- **สัญญาณ:** ลูกค้าตั้งราคาขาดทุนเพราะเครื่องคำนวณ · ร้องเรียนคำแนะนำผิด

## 🟢 R8 — Vendor lock-in Firebase
- **วิธีกัน:** สเกลนี้ Firebase คุ้ม ยังไม่ต้องแก้ · แค่กัน business logic ไว้ใน Functions ไม่ปนกับ vendor SDK มากเกิน
- **สัญญาณ:** ต้นทุน Firebase พุ่งผิดปกติ · ชน quota บ่อย

# ROADMAP — แผนทำให้เสร็จสมบูรณ์

> หลักการจัดลำดับ: **ปิดรอยร้าวฐานรากก่อน → ปิดงานค้าง → สร้างตัวต่าง → ปล่อยจริง**
> ห้ามสร้างของใหม่บนฐานที่รั่ว · ทีม = โซโล่ (Roger + Claude) → ทุก Phase ตัด scope ให้ทำไหว
> เกณฑ์ checklist: ✅ เสร็จ+เทสต์ · 🟡 มีโค้ดยังไม่เทสต์ · 🔴 ยังไม่เริ่ม · ขนาดงาน: S/M/L

---

## 🎯 วิสัยทัศน์ผลิตภัณฑ์ (Roger 2026-08-25) — แพลตฟอร์มทั่วไป ไม่ผูกเกษตร

**หน้าบ้าน (แอปผู้ใช้) = 3 เสา:**
- **Community** — ฟีดสังคม: โพสต์ / ถามปัญหา / รีวิวสินค้า
- **Activity** — เครื่องมือกิจกรรม–ลอยัลตี้ที่ **ร้านสร้างเองได้แบบยืดหยุ่น**: สะสมแต้มจากกิจกรรม + จากการซื้อ + **สุ่มจับรางวัล** + ชาเลนจ์ (ต่อยอด points/badges/tier/leaderboard ที่มีอยู่) — "ไว้เล่นกับลูกค้า"
- **Market** — ร้านค้า (โฉมใหม่ กำลังเลือกแนว A/B/C)

**หลังบ้าน (แอดมิน)** = บริหารจัดการครบทุกอย่าง

**หลักการ:** ระบบต้อง **ยืดหยุ่น รองรับแบรนด์สินค้าอื่นในอนาคต** ไม่ hardcode เฉพาะพืช/เกษตร (ใช้ `terms`/categories/flags ต่อแบรนด์)

---

## Phase 0 — ฐานราก & ความปลอดภัย 🔴 *(ต้องก่อนทุกอย่าง)*

**ทำไมก่อน:** isolation ข้ามแบรนด์คือความเสี่ยง "จบเกม" (R1) — ทำงานจริงแต่ **ไม่มีเทสต์ + legacy rules ระดับ root ยังไม่ลบ** · และมีบั๊ก settings แยกต่างหาก (fail-silent) ที่ทำให้ตั้งค่าเพี้ยน · ปล่อยลูกค้าที่ 2 ไม่ได้จนกว่าจะอุด

- [x] **แก้บั๊ก settings — รวม 2 คลังให้เหลือคลังเดียว** (`settings/app`+`settings/features`) : เปลี่ยน `saveBranding`/`setFeature` ให้เขียนคลัง canonical + ลบ listener phantom + ขยาย listener `settings/app` ให้รับ tierLabels/navLabels — ✅ deploy + เทสต์ 3 ข้อผ่าน (2026-08-24)
- [x] ลบ legacy rules top-level ที่ `firestore.rules:102-150` — ✅ deploy + ตรวจ 3 ข้อผ่าน (2026-08-24) · เหลือ top-level แค่ `/tenants`, `/tenantRequests`, catch-all deny
- [x] เขียน **rules-test** (`tests/rules.test.js`, 15 เคส): อ่าน/เขียนข้ามแบรนด์=denied · guest เขียนไม่ได้ · แก้ points/tier เอง=denied · tenant admin ไม่ข้ามแบรนด์ · root path ปิด — ✅ **รันจริงผ่าน 15/15** (emulator, 2026-08-24)
- [x] ต่อ rules-test เข้า CI — `.github/workflows/rules-test.yml` รันทุก push/PR · ✅ **CI เขียวบน GitHub** (Java 21, 15/15, 2026-08-24)
- [x] push ขึ้น GitHub (deployed == repo) — ✅ commit `2456475`+`ef9d4b9` บน main (แก้ R4 divergence)

> ✅ **Phase 0 ปิดครบ (2026-08-24)** · งานเก็บที่ยังค้าง (ไม่บล็อก): workflow `firebase-deploy.yml` fail มาตั้งแต่ มิ.ย. (secret ไม่ครบ — deploy มือแทน), orphan cleanup (`public/`, root `index.js`)
- [ ] Snapshot baseline สะอาด + ยืนยัน deployed == repo — **S**
- [ ] เปลี่ยนวิธีทำงาน: เลิก `apply_*.js` → แก้ไฟล์ตรงๆ — **S**

**DoD:** rules-test เขียวใน CI · ตั้งค่าแบรนด์ A ไม่กระทบ B · deployed ตรง repo

---

## Phase 1 — ปิดงานค้าง "น่านน้ำ" 🟡 *(อย่า gold-plate)*

**เป้า:** ครึ่ง blue ocean ที่ทำไป ~80% ให้จบ 100% · กฎ: **ทำให้จบ หรือลบทิ้ง** ห้ามค้างครึ่งๆ

- [ ] Badges (STUB) → **ตัดสินใจ:** ทำ logic แจกจริง หรือ **ลบทิ้ง** (แนะนำลบก่อน) — **S–M** · *(Roger เลือก "สร้างจริง" — ยังไม่ทำ ทำต่อจาก Groups)*
- [x] Community groups → **สร้างจริง (มี join) + คุมเปิด/ปิดต่อแบรนด์** (ไม่ใช่ลบ flag) — ✅ build + rules-test 22/22 เขียว (2026-08-24) · รอ deploy · Leaderboard กลายเป็น toggle ต่อแบรนด์ด้วย
- [x] ทดสอบ end-to-end ร้านค้า — ✅ **ผ่าน (2026-08-24)**: ร้าน→ตะกร้า→เช็คเอาท์→กรอกที่อยู่→สร้างออเดอร์+PromptPay QR→เก็บ Firestore→admin ลบ/จัดการได้ (verified live via Chrome + JS) · เหลือ upload สลิป+เปลี่ยนสถานะ = lifecycle เดิม · **โบนัส:** เปลี่ยน alert/confirm ทั้งหมดใน index.html → toast/confirmModal (UX ลื่น + ไม่บล็อกหน้าจอ)
- [x] ขัดเงา tenant onboarding — ✅ **"จุดไฟชุมชน" (2026-08-24)**: seedTenant เพาะกลุ่ม+โพสต์ต้อนรับ+ชุดตราอัตโนมัติ · Dashboard มี checklist ตั้งค่า + ปุ่มโพสต์เริ่มบทสนทนา 1 คลิก — แบรนด์ใหม่เปิดมาไม่ร้าง

**DoD:** ไม่มีฟีเจอร์ครึ่งๆ เหลือ · สร้างแบรนด์ใหม่แล้วขายได้ครบ flow

> ✅ **Phase 1 ปิดครบ (2026-08-24)** — Groups (join) + Badges สร้างจริง · e2e ร้านผ่าน · onboarding "จุดไฟชุมชน" · toast แทน alert/confirm · ทั้งหมด deploy + push แล้ว

---

## Phase 2 — ตัวต่าง: ต้นทุน–ราคา + ผู้ช่วยการตลาด 🔴 *(greenfield = "ทำไมต้องเรา")*

**เป้า:** จาก "community+shop อีกเจ้า" → "โรงงานที่ช่วยลูกค้าขายเก่งขึ้น" · เริ่ม **v1 เบาสุด**

- [x] แปลงราคาสินค้าเป็น **ตัวเลข** — ✅ **(2026-08-25)** `parsePrice()` ฝั่ง admin เก็บ number ทั้งเพิ่ม/แก้/CSV · ปุ่ม migrate แปลงข้อมูลเก่าทั้งหมด · deploy แล้ว
- [x] **เครื่องมือต้นทุน–ราคา v1:** — ✅ **(2026-08-25)** หน้า "🧮 คิดราคา–กำไร" ในแอดมิน: กรอกต้นทุนโรงงาน+ต้นทุนอื่น+กำไรเป้าหมาย → ราคาขายแนะนำ + **กำไรจริงต่อชั้นสมาชิก** (ผูกส่วนลด tier 0/5/10/15%) + จุดคุ้มทุน 2 เส้น (เท่าทุน / ขั้นต่ำปลอดภัย) · เพิ่ม field `cost` ต่อสินค้า + คอลัมน์กำไรในตาราง + ปุ่มคิดจากสินค้า · deploy แล้ว · *หมายเหตุ: verify สดฝั่งแอดมินไม่ได้ (ต้อง login) — ผ่าน static check + ตรวจเลขมือ*
- ⏸️ ~~**ผู้ช่วยการตลาด v1**~~ — **เลื่อนออก (Roger ตัด 2026-08-25)** ไป Phase 4 · reuse Gemini (`analyzePlant`) ทำ text-gen คำโปรย/แคปชัน/โปรโมชัน — ยังไม่ทำตอนนี้

**DoD:** ลูกค้ากรอกราคาโรงงาน → เห็นราคาขายแนะนำ+กำไร ✅

> ✅ **Phase 2 ปิด (2026-08-25)** ตาม scope ที่ตัดแล้ว — ราคาเป็นตัวเลข + เครื่องมือต้นทุน–ราคา–margin · ผู้ช่วยการตลาด AI เลื่อนไป Phase 4

---

## Phase 3 — ปล่อยจริง + วัดผล 🟢

**เป้า:** พิสูจน์ว่าลูกค้า *ใช้จริง* ไม่ใช่แค่มีของ

- [x] Feature flag ต่อแบรนด์ — ✅ **(2026-08-25)** เพิ่ม flag `pricingTool` (default **ปิด**) คุมเมนู+หน้าคิดราคา+ปุ่มในสินค้า ต่อแบรนด์ · toggle ที่หน้า Features · gate ครบ (menu/screen/button) เปิด-ปิดเห็นผลทันที · deploy แล้ว · *(โครง flag เดิม `settings/features` per-tenant + listener สดใช้ได้อยู่แล้ว — งานนี้ต่อยอด gate ของใหม่ + default-off สำหรับ pilot)*
- [ ] dogfood DemeterRich ก่อน → ลูกค้านำร่อง 1 ราย — **—**
- [ ] วัดผลที่ถูกต้อง: ลูกค้า **ใช้จริง + สั่งผลิตเพิ่ม** (ไม่ใช่ยอด user) — **—**

**DoD:** ลูกค้าแบรนด์ที่ 2 ใช้งานจริงครบ loop โดยข้อมูลไม่รั่ว

---

## Phase 3.5 — ตัด + จัดโครง 3 เสา + de-agri 🟡 *(กำลังทำ 2026-08-25)*

**เป้า:** ทำให้แอปเบา เป็นกลาง (ไม่ผูกเกษตร) และจัดหน้าบ้านเป็น Community / Activity / Market

- [x] ปิดกลุ่มย่อย — config default `communityGroups:false` (opt-in) · ⚠️ DemeterRich ตั้ง true ไว้ใน Firestore → ต้อง uncheck ในแอดมิน หรือเซ็ตผ่าน ADC
- [x] ปิด "ใกล้ฉัน" — config default `proximityAlert:false` คุมรวม (แท็บใกล้ฉัน + step GPS onboarding + แถบแจ้งเตือน + bullet landing) เก็บโค้ด · ⚠️ DemeterRich ต้องเซ็ต false เหมือนกัน
- [x] **de-agri (เบา)** — ✅ ตัดหมอพืช AI (ทุก entry) + crops (โปรไฟล์/onboarding) + bullet/tagline/ป้ายเมนู เป็นกลาง (2026-08-25) · หมอพืช AI backend เก็บโค้ดไว้ (dormant)
- [x] **จัดโครงเมนู 3 เสา** — ✅ **(2026-08-25)** Community(ชุมชน/feed) · Activity(กิจกรรม/แต้ม+อันดับ) · Market(ร้านค้า) · Profile · icon เส้น · verified สด
- [x] **รื้อ Profile** — ✅ ตัด crops/หมอพืช + การ์ดที่อยู่จัดส่ง (→checkout auto-fill) + ประวัติการสั่งซื้อ
- [x] **โฉมใหม่การ์ดร้าน (Market)** — ✅ 3 preset เลือกต่อแบรนด์ (อบอุ่น/สะอาด/คึกคัก) ที่ตกแต่งร้าน

> ✅ **Phase 3.5 ปิดครบ (2026-08-25)** — เหลือ: DemeterRich uncheck communityGroups ในแอดมิน (ข้อมูล)

---

## Phase 4 — Activity Engine (เครื่องมือกิจกรรม–ลอยัลตี้) 🔴 *(ฟีเจอร์ใหญ่ ตัวชูโรง)*

**เป้า:** ให้ร้าน **สร้างกิจกรรมเองได้แบบยืดหยุ่น** ไว้เล่นกับลูกค้า (ต่อยอด points/badges/tier ที่มี)

- [ ] Campaign builder ในแอดมิน: ตั้งกิจกรรม + เงื่อนไขได้แต้ม (จากกิจกรรม / จากการซื้อ) — **L**
- [ ] **สุ่มจับรางวัล / lucky draw** (ใช้แต้มแลกสิทธิ์สุ่ม, แจกรางวัล server-side ตรวจสอบได้) — **L**
- [ ] ชาเลนจ์ / ภารกิจ + แสดงผลในเสา "Activity" หน้าบ้าน — **M**

---

## Phase 5 — เติบโต (เลื่อนไว้ก่อน)

**ผู้ช่วยการตลาด AI** (เลื่อนจาก Phase 2 — 2026-08-25) · Document engine · Reorder autopilot · PMS connector สด · Payment gateway อัตโนมัติ · IG-style templates · เก็บเงินรายเดือน (SaaS จริง)
→ ทำเมื่อฐานนิ่งและมีลูกค้าใช้จริงแล้วเท่านั้น

---

## Dependency (ห้ามสลับ)

- **Phase 0 = รากของทุกอย่าง** ห้ามข้าม (ปล่อยลูกค้าที่ 2 ไม่ได้ถ้า isolation ยังไม่มีเทสต์)
- Phase 2 "ต้นทุน-ราคา" ต้องแปลงราคาเป็นตัวเลขก่อน (งานแรกใน Phase 2)
- Phase 3 pilot ต้องรอ Phase 0 (ปลอดภัย) + Phase 1 (onboarding ลื่น)
- Phase 2 ทำคู่ขนานกับปลาย Phase 1 ได้ ถ้ากำลังเหลือ
